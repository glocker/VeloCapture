import { View, Text } from "react-native";

export default function Leaderboard() {
    return (
        <View style={{ flex:1, alignItems: "center", justifyContent: "center" }}>
        <Text>Top invaders</Text>
        </View>
    );
}