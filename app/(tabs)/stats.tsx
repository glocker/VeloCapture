import { View, Text } from "react-native";

export default function Stats() {
    return (
        <View style={{ flex:1, alignItems: "center", justifyContent: "center" }}>
        <Text>Statistics (cells, km², total distance)</Text>
        </View>
    );
}